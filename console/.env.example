# The console talks to the gateway through a same-origin proxy (Vite in dev, nginx
# in the Docker image), so no API base URL is needed by default. Override only if
# you serve the API from a different origin.
# VITE_API_BASE_URL=http://localhost:8080
