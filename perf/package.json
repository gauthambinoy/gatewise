{
  "name": "auvex-perf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Load-testing harness and OpenAPI<->SDK contract tests for the Auvex gateway.",
  "scripts": {
    "load": "node load.mjs",
    "contract": "node --test contract.test.mjs",
    "test": "node --test contract.test.mjs"
  },
  "dependencies": {
    "autocannon": "^7.15.0",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
