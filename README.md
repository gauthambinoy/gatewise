# Auvex

A drop-in AI gateway that redacts, routes, governs, and logs every LLM call an enterprise makes — change one URL and every prompt flows through a controlled, audited passage.

> **Status: scaffolding.** Build in progress. This README is filled in from a real run once the core is shippable (see CLAUDE.md §7a). No staged screenshots, no fake data.
