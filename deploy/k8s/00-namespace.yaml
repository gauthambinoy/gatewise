# Namespace for all Auvex objects. `kustomization.yaml` also sets this as the
# target namespace, so every resource lands here on `kubectl apply -k`.
apiVersion: v1
kind: Namespace
metadata:
  name: auvex
  labels:
    app.kubernetes.io/part-of: auvex
